export default function TopRatedPanel() {
  return (
    <div className="bg-surface rounded-xl p-5 border border-white/5 text-sm text-muted">
      Ranked purely by audience rating across the whole catalog — the same
      list every visitor sees, no personalization involved. Useful as a
      baseline: any personalized mode should beat this on relevance for a
      given viewer, or it isn't earning its complexity.
    </div>
  );
}
